[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "nodejs", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "python3 -m pip install -r requirements.txt",
  "npm install"
]

[start]
cmd = "python3 bot.py"
